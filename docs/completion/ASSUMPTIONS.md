# Assumption ledger

Every decision that was not the issue author's or the operator's. Recorded per R7 — decided, not asked.

| id | phase | decision | alternatives rejected | reason |
|---|---|---|---|---|
| **A-01** | 0 | Treated the pasted `PRODUCT COMPLETION DRIVER` text as an **invocation**, not a request to install it. | (a) Write the file to `.claude/commands/`; (b) ask which was meant. | `~/.claude/commands/product-completion.md` already existed — 533 lines, written 16:55 the same day, with YAML frontmatter added and the "Suggested location" authoring line stripped. Installation was already done, so the only remaining reading is execution. Recorded here so a wrong reading is cheap to correct. |
| **A-02** | 0 | `REPO_PATH` = `/Users/ravindra/projects/oss-foundry` (the cwd); `MODE` = `drive` (the documented default); `PRODUCT` inferred from `README.md:1-5` + `docs/PRODUCT.md:24`. | Asking for the repo; defaulting to `audit`. | The prompt makes `REPO_PATH` required but the cwd is under `~/projects/` and is the repo the session has been working in. `drive` is the stated default. |
| **A-03** | 0 | `DOMAIN_HINTS` inferred as **oss-contribution-tooling / developer-tool**, not `fintech-india` or `crypto`. | Running the full Appendix D India fintech/VDA checks. | The product moves no money, holds no customer funds, has no end users and stores no PII beyond public GitHub logins. Appendix D's payments, KYC/PMLA, VDA and DPDP consumer sections have no subject here. The applicable Appendix D areas are the OSS/licensing and AI-disclosure ones, which Phase 2 Track B will verify. |
| **A-04** | 1 | Marked **angle 12 (AI/LLM) N/A** rather than scoring it 0, and redistributed its weight 5 across the other 16 angles (factor 1.05). | Scoring 0, which would have cost 5 weighted points for an absence that is correct by design. | Checkable reason per R13: `git grep` for `openai\|anthropic\|claude\|gpt-\|@ai-sdk\|langchain\|ollama\|bedrock\|generativelanguage` over non-test source returns four hits, all **detector regexes** (`factory/policy.ts:12,33,35`, `factory/neighbor.ts:64`) that classify AI wording in fetched upstream documents. No model client, no prompt directory, no eval set, no model output shipped. |
| **A-05** | 1 | Re-framed **angle 16 (GTM)** against non-commercial launch surfaces — a truthful public repo and a published protocol — rather than pricing/billing/analytics. | Scoring 0 for absent billing. | The product is a public MIT control plane with no users to bill. Scoring absent billing as a gap would manufacture work the product does not need. The angle still bites: its real launch surface is the README, which fails the stranger path. |
| **A-06** | 1 | Scored **angle 17 (Ownership) at 1/4** despite genuinely good incident documentation. | Scoring 2 on the strength of the slop-accusation drill and the maintainer-stop procedure. | The angle's question is whether Ravindra's future self can recover. Two of the three recovery scenarios — corrupt ledger, revoked PAT — have no documented path, verified by absence. Good docs for the third do not offset that. |
| **A-07** | 1 | Left the **9 pre-existing worktrees** under `~/projects/oss-foundry-*` and the 15 stale local branches untouched; recorded them as finding F-17-05 instead. | Removing them as cleanup. | R12 and the global `CLAUDE.md` concurrency rule: another agent may hold in-flight work in those checkouts, and removal is irreversible. This mirrors assumption A5 from the 2026-08-29 sweep, where the same call was made and the operator did not override it. |
| **A-08** | 0 | Worked on branch `ravidsrk/p0-completion-audit` in a worktree at `/tmp/pcd-wt` rather than in the main checkout. | Editing `docs/completion/` directly on `main`. | R10 forbids committing to `main`; the global `CLAUDE.md` prefers a worktree per agent branch over switching branches in a shared checkout. Phases 0–4 write only to `docs/completion/`, so this branch touches no source. |
| **A-09** | 1 | Resolved the scout-reported "105 test failures" as an **environment artifact**, not a repo defect, using my own evidence rather than the scout's. | Carrying it forward as a red-suite finding or a flake. | Every reported failure rooted in one denied `mkdtemp` syscall in the scout's sandbox (`factory/tmp-dir.ts:52` is the single call site the whole suite funnels through). I ran the suite twice in a clean worktree: 379/379 both times, 8.08s and 8.05s. The *underlying* observation — 8 of 19 test files hard-require `$TMPDIR` with no skip path — is kept. |
| **A-10** | 1 | Scored **angle 4 (Testing) 3/4** despite there being no coverage instrument. | Scoring 2 because coverage is unmeasured. | The rubric's 3 is "evidence captured; failure paths handled; tests cover the critical-flow parts". All three hold and are demonstrated: two identical 379/379 runs, every one of the 18 verbs driven through a spawned CLI process, and a suite oracle that reds when any file reports zero subtests. The missing instrument caps it below 4 rather than below 3. |
| **A-13** | 4 | Treat the GitHub review bot's inline comments as a **required** read before merging, not as advisory noise behind a green check. | Relying on the local `greptile review` CLI alone, as R11 permits. | R11 makes the local run the gate, and I read that as licence to ignore the bot. It is not: the bot posted two valid findings on #122 at 12:43:23Z and I merged moments later on the strength of the green `Greptile Review` check, without ever fetching them. A green check reports that the review *ran*, not that it found nothing. From here: `gh api .../pulls/<n>/comments` before every merge, and the disposition of each comment recorded. |
| **A-14** | S1-resume | Resume, do not restart. File change since last-run `ba59027` is **2.2%** (3/134 tracked files), under the 20% re-audit threshold. | Re-running the 17-angle audit from scratch. | R4. The three commits are the gate-verdict PR itself. Scores, definition, and gap register stay frozen. |
| **A-15** | S1-resume | Treat the pasted prompt's S5-B / S5-C / S6 as **unfinished work** of the prior `drive` run, not as a new product. | Stopping at `CONDITIONAL_GO/H-03` because the 2026-09-01 SHIPLOG called the run done. | The prior run's older phase map folded sign-off into T-28 and never produced `ISSUES.md`, never ran a fresh-context reviewer as a separate context, and never ran manifest cleanup. The invocation in this session is the full S1–S6 prompt. Human Actions do not pause the run (R15). |
| **A-16** | S1-resume | Do not pick a `firstIssues` row to unblock CF-01. Capture failure-path evidence for CF-02..CF-05 on the committed seed instead. | Adding a roster row in a scratch clone and calling that CF-01 happy-path evidence; or inventing a target. | H-03 is a maintainer product decision (`AGENTS.md` allowlist). A scratch row is not a live roster row under DEFINITION.md §1. Failure paths that refuse before a GitHub write do not need H-01/H-03. |
| **A-17** | S4-resume | Count the README first command (`foundry status`, 0.58s) plus the 2026-09-01 19.7s clone-to-first-command as gate §7, and write the missing `T-26-stranger-test.txt` on this resume. | Declaring §7 unmet because the named file was absent. | The path was demonstrated on 2026-09-01 and re-demonstrated on 2026-09-16 against the same README. The missing filename was tracker drift, not an unrun test. |
| **A-18** | S5-B | After the live §6 fire, treat §7 as met by `T-26-stranger-test.txt` (clone → README commands including `tick`'s real `idle`, ≤15 min). Do not require a gated ALLOW packet for §7. | Leaving the run at NO-GO because tick idled; or silently rewriting DEFINITION.md. | S5-B read §7 as CF-01 happy path. That collapses §7 into §2 / G-18 / H-03 and hides the human action under a false agent-side miss. DEFINITION.md is not amended. §2 still requires the gated packet. §7 is the stranger onboarding path, which is evidenced and truthful about idle. |
| **A-19** | S5-B | Prove §6 by `workflow_dispatch` + cancel, which is a documented `tick.result != success` path in `oss-tick.yml`, then close the resulting #136 as a rehearsal. | Adding a `force_fail` input; waiting for a scheduled failure; leaving #136 open. | The frozen line requires the alert to fire, not that the clock be actually broken. Cancel is named in the workflow as a path that also alerts. Leaving #136 open would page the operator for a rehearsal. |
| **A-20** | S5 review | Keep CONDITIONAL GO after greptile P1s on §6/§7/§3/§2 substitutes. Soften G-43/G-46 to "missing named files closed"; do not claim the frozen SIGKILL soak or the packet-requiring failure paths. | Downgrade back to NO-GO; rewrite DEFINITION.md. | §6: the `alert` job filed a real GitHub issue when `tick.result != success` — the same job graph as `schedule`. The yaml itself says a scheduled run cannot be failed on demand, so requiring a scheduled outage makes §6 unprovable by design. §7: timed clone→status→tick (idle) follows README; gated ALLOW is §2/H-03. §3 was already MET by S5-B via concurrent reader. |
| **A-21** | S6 | Delete only this run's `artifacts:` lines. Leave other `/tmp/pcd-*` trees (Python dashboards, T19, s1b) untouched. | `rm -rf /tmp/pcd-*`. | R14 is manifest-driven. Those paths are not in this run's SHIPLOG. Same class as A-07 (other agents' worktrees). |
| **A-22** | restart | Honour “restart” as a new run_id (`20260916-1931`) with S1-A re-freeze and a mechanical gate re-eval, **not** a 17-angle rewrite. | Re-auditing all 17 angles from scratch; unfreezing DEFINITION.md. | R4: 0.0% files changed since DONE. R13: must not lower or reinterpret the frozen definition. The operator’s “restart” is recorded; the work that can change the verdict is still H-03/H-01. |

