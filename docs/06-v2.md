# v2 — scout, sandbox, scorecard

v2 is implemented in this repo as real modules. Credentials stay out of git.

## Grok scout

`src/lib/foundry/scout-ai.ts` — server function, model `grok-4.5`, `max_tokens: 280`, temperature 0.

- User-initiated only (button on a packet). Never on page load, never in the GHA loop.
- Returns `{ grok: 0-20, rationale, risk }`. Heuristic score still wins the floor; Grok is an overlay.
- If `XAI_API_KEY` is missing, the UI says so and the heuristic stands.

## Live GitHub scout

`src/lib/foundry/github-scout.ts` — user-initiated fetch of open issues on Wave 0–1 allowlisted repos. Public GitHub API; `GITHUB_TOKEN` / `GH_TOKEN` on the operator host raises the rate limit. Never on page load. Queueing still obeys one-in-flight.

First Wave 0 packet executed: [orca-fleet#70](https://github.com/ravidsrk/orca-fleet/pull/70) (release 0.5.0, fixes #42). **Merged by the maintainer** 2026-08-27. Foundry-attested Wave 0 merge **1/2**.

Follow-up (2026-08-26): Greptile flagged the `0.5.0` heading as future-dated (`2026-08-27` vs UTC commit day). Folded to `2026-08-26` in `d91fe2f`, replied, resolved the thread. Greptile returned 5/5. Maintainer merged the next morning.

Second Wave 0 packet: [frontguard#196](https://github.com/ravidsrk/frontguard/pull/196) (draft, README architecture vs the monorepo, fixes #195). Greptile 5/5, no review threads. CI red is pre-existing on `main` (playwright lockfile + setup-node drift from dependabot). Marked quiet. Foundry does not merge.

Third Wave 0 packet: [orca-fleet#72](https://github.com/ravidsrk/orca-fleet/pull/72) (draft, unreadable SKILL.md must not abort the catalog, fixes #71). 5 files, +72/−15, 103 tests. Wave 1 (#1476) stays parked until two Foundry-attested Wave 0 *merges*.

The console Follow-up station syncs the live PR (user-initiated) and records bot-reconcile notes. `followed-up` means threads are answered, not that we merged. `merged` is recorded only after a maintainer merge.


## E2B sandbox

`src/lib/foundry/sandbox.ts`

| Wave | Provider |
|---|---|
| 0 | Host worktree |
| 1–2 | E2B (Daytona allowed as alternate image) |

Lifecycle: create → shallow clone → test command → harvest patch → destroy.

Hard rules:

- No GitHub App private key in the box.
- No SSH keys, npm tokens, or `.env`.
- Network allowlist: git + package registries.
- If tests cannot run, park. Do not skip the oracle.

The console runs this lifecycle as a **dry-run** so you can see the command transcript without spending E2B credits. Wire `E2B_API_KEY` in the worker host (not this preview) to make it live.

## Scorecard halt

`src/lib/foundry/scorecard.ts`

Halt a repo (`stop`) when:

- maintainer tone is `banned`, or
- any revert of our patch, or
- opened ≥ 3 and merge rate < 40%.

Watch when tone is `cold`, or opened ≥ 2 and merge rate < 60%.

A `stop` repo is treated as denylisted until a human removes the halt in `allowlist.yaml`.

## GitHub App (operator host)

Permissions (least):

- Contents: read on upstream, write on operator forks.
- Pull requests: write (draft).
- Issues: read.
- Checks: read.
- Metadata: read.

No administration. No merge. No Actions write on upstream.

## Promotion rule

v2 features may run on Wave 0 immediately. They may run on Wave 1 after **two merged Wave 0 packets** in this factory (the historical oss-contribute run counts as evidence of the *mission*, not of this control plane — Foundry’s own counter starts at the seed of 5 merged on orca-fleet and still requires a Foundry-attested freeze for #42).
